spring:
  application:
    name: mobile-gaming-management
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/mobile_gaming_management}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:102938Sm@#}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  jackson:
    default-property-inclusion: non_null
server:
  port: ${PORT:8080}
  servlet:
    context-path: /api/v1
app:
  jwt:
    # Replace in production with a random Base64 value of at least 64 bytes.
    secret: ${JWT_SECRET:bW9iaWxlLWdhbWluZy1tYW5hZ2VtZW50LWRlZmF1bHQtc2VjcmV0LW11c3QtYmUtY2hhbmdlZC1pbi1wcm9kdWN0aW9uLTIwMjY=}
  cors-origins: ${CORS_ORIGINS:http://localhost:3000,http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:8081}
management:
  endpoints:
    web:
      exposure:
        include: health,info
